- name: variables from local inventory file 
  hosts: local 
  connection: local 
  tasks: 
  - name: print the course information
    ansible.builtin.debug: 
      msg: Course name is {{ COURSE }}, duration is {{ DURATION }}, trainer name is {{ TRIANER }}